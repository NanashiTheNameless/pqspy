{
  "name": "pqspy-namelessnanashi",
  "private": true,
  "description": "Tooling to build and publish the PQSpy add-on; the extension itself is plain JS in src/.",
  "scripts": {
    "build": "web-ext build --overwrite-dest",
    "lint": "web-ext lint"
  },
  "devDependencies": {
    "web-ext": "^10.6.0"
  }
}
